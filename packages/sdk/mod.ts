/** This is not really the first version of the package. Just learning JSR as the intented target for the Kitledger SDK distribution channel */
export function add(a: number, b: number): number {
  return a + b;
}